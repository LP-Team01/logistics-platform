server:
  shutdown: graceful

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s

management:
  endpoint:
    health:
      show-details: never

logging:
  level:
    root: INFO
    com.logistics: INFO
